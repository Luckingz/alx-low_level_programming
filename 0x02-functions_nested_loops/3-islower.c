#include "main.h"

/**
 * _islower - A programme that that checks for lower case
 * Return: 0 on success
 */

int _islower(int c)
{
	int r;

	if (r == 'o' && r == 108)
	{
		return (1);
	}
	else return (0);
}
