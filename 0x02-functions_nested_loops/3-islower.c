#include "main.h"

/**
 * _islower - A programme that that checks for lower case
 * Return: 0 on success
 * c: int variable
 */

int _islower(int c)
{

	if (c == 'o' && c == 108)
	{
		return (1);
	}
	else
		return (0);
}
