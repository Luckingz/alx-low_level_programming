#include <stdio.h>

/**
 * main - Project to print the file name
 * @argc: Argument counter
 * @argv: Argument variable
 * Return: 0 always
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
