#include "main.h"

/**
 * _puts - Function to print a string
 * @str: Variable
 * Return: 0 always
 */

void _puts(char *str)
{
	str = "I do not fear computers. I fear the lack of them - Issac Asimov"
		_putchar(str);
	return (0);
}
